# AI Project Board — MCP Server + 唯讀 Web 看板

Claude Desktop 透過本機 MCP server（stdio）把 AI 規劃出來的專案與任務寫進資料庫看板；
同一個行程也用內嵌 Tomcat 提供唯讀 REST API + SSE，給一個零建置的 Vue 3 前端即時顯示進度。

## 架構：單一行程同時提供 stdio MCP 與 HTTP（Plan A）

R2 的 Step 0 已驗證：`spring.main.web-application-type: servlet` 搭配
`banner-mode: off` + `logging.pattern.console: ""`，內嵌 Tomcat 的啟動輸出
**不會**污染 stdio 的 JSON-RPC 通道。已用實機測試確認：

- stdio 端 `initialize` / `tools/call` 全部回應乾淨的 JSON-RPC，stderr 0 行
- 同時間 `curl http://localhost:8080/api/ping` 正常回應 200

因此採用 **Plan A**：不拆分行程、不需要 outbox 表、不需要 H2 TCP server 模式。
`BoardEventPublisher` 介面的實作是 `InProcessEventPublisher`（包
`ApplicationEventPublisher`），事件經由 `@TransactionalEventListener(AFTER_COMMIT)`
在交易真正 commit 後才推送給 SSE，已用「更新不存在的 taskId 導致例外」驗證過：
失敗的交易不會讓事件洩漏到 SSE 上。

**限制（規格書明定，未來 R3 才處理）**：

- 看板只在 Claude Desktop 啟動這個 MCP server 期間存活，行程結束畫面就跟著沒了。
- 本輪固定單機、單一 H2 file、單一 Spring Boot 行程。**不要**額外手動再啟動一份
  jar，會跟 Claude Desktop 啟動的那份搶 8080 port 與 H2 檔案鎖。
- 兩台機器各跑一份 = 兩個獨立資料庫，不會同步，這跟有沒有用 Docker 無關。

## 資料庫：H2（非規格書原定的 SQLite）

Flyway 自 10.x 起已完全移除 SQLite 支援（`flyway-community-db-support` 模組不含
SQLite，Maven Central 查無任何 `org.flywaydb` 下的 SQLite artifact）。改用 H2
（file 模式）：對 Flyway、標準 SQL 語法的支援完整，SQL 方言也更貼近 MySQL，
對 R3 遷移反而更有利。

Migration 只用標準 SQL 的 `GENERATED BY DEFAULT AS IDENTITY`（不用 H2 專屬語法，
如 `RANDOM_UUID()`）。R3 換 MySQL 時，理論上只需改 `application.yml`（datasource
url/driver）與 `V1__init_schema.sql`（identity 語法換成 `AUTO_INCREMENT`），
Service 與 Repository 一行都不用動。

`./data/` 已加入 `.gitignore`，資料庫檔不進版控。

## 設定一律外部化（R2 起）

`application.yml` 裡**不得出現任何寫死的路徑、port、或連線字串**，一律
`${VAR:預設值}` 形式：

```yaml
spring:
  datasource:
    url: ${BOARD_DB_URL:jdbc:h2:file:/絕對路徑/data/board;DB_CLOSE_ON_EXIT=FALSE}
    username: ${BOARD_DB_USER:sa}
    password: ${BOARD_DB_PASSWORD:}
server:
  port: ${BOARD_PORT:8080}
logging:
  file:
    name: ${BOARD_LOG_FILE:/絕對路徑/logs/board.log}
```

預設值本身仍是本機開發用的絕對路徑（見下一節原因），R3 容器化時改用環境變數整個蓋掉即可。

## `application.yml` 的 db/log 路徑預設值必須是絕對路徑

`BOARD_DB_URL` / `BOARD_LOG_FILE` 的**預設值**不能用相對路徑（`./data/...`）。
實測發現 Claude Desktop 啟動這個 MCP server 子行程時，工作目錄**不是**專案根
目錄（很可能是 `/`），H2 對相對路徑解析出來的目錄若無寫入權限，會在 Logback
接管前直接把例外訊息印到 stdout，把 JSON-RPC 協定弄髒，導致 Claude Desktop 端
出現「Unexpected token ... is not valid JSON」「Server disconnected」。已用
`cd / && java -jar ...` 重現過這個錯誤（`Read-only file system` 寫入 `/data`），
改絕對路徑後同樣情境下 stdout 完全乾淨。

**換機器、換帳號、或搬動專案資料夾時**，同步修改 `application.yml` 裡這兩個
預設值（或透過 `BOARD_DB_URL`/`BOARD_LOG_FILE` 環境變數覆寫），並重新
`./mvnw clean package`。

## 專案結構（R2）

```
src/main/java/dev/aiboard/
├── DashboardApplication.java
├── config/        McpToolConfig
├── mcp/           ProjectBoardTools（三個既有 tool + update_task_status）
├── project/       Project, ProjectRepository, ProjectService
├── task/          Task, TaskLog, TaskStatus, TaskRepository, TaskLogRepository, TaskService
├── common/        BoardException, TaskCategory
├── event/         BoardEvent, BoardEventPublisher, InProcessEventPublisher
└── web/           ProjectQuery, ProjectListController, BoardQuery, BoardController,
                   EventStreamController, SseEmitterRegistry
src/main/resources/
├── application.yml
├── db/migration/
└── static/        index.html, tokens.css, app.js（Vue 3 CDN，無建置）
```

Package 已從 R1 的 `tw.danielhuang.dashboard` 全面更名為 `dev.aiboard`；
`artifactId` 為 `ai-project-board-backend`，jar 檔名
`ai-project-board-backend-1.0.0.jar`。

## 唯讀原則

**`web/` 層不得新增任何寫入端點。** `/api/projects`、`/api/projects/{id}/board`、
`/api/projects/{id}/tasks/{taskId}/history`、`/api/events`（SSE）全部是
`GET`，沒有任何 POST/PUT/DELETE。所有寫入只能經由 MCP tool。這是刻意的範圍
限制，之後如果要加寫入端點（例如網頁上拖拉改狀態），要先確認是否真的要打破
這條原則，而不是預設可以加。

## 分層規則

- `mcp/`（`ProjectBoardTools`）只能依賴 `*/Service`，**不得注入 Repository**，
  不得寫任何業務邏輯，只做參數解析、呼叫 Service、把結果轉成回傳字串。
- `Service` 之間可互相呼叫，但 `TaskService` 不得反向依賴 `ProjectService`
  以外的東西。
- `Service` 層不得出現任何 MCP 相關型別，必須能在沒有 MCP 的情況下被單元測試
  呼叫（`ProjectServiceTest` / `TaskServiceTest` 皆為純 Mockito 單元測試）。
- `web/` 層的 `ProjectQuery` / `BoardQuery` 是唯讀查詢類別，可以直接注入
  Repository（跟 `mcp/` 的限制不同），因為這裡本質是 read model，不是業務邏輯。
- Entity 不外洩到 `mcp/` 或 `web/`，Service 一律回傳 record DTO。
- Migration 只用標準 SQL；日期時間欄位一律 `TIMESTAMP`，主鍵一律 `BIGINT`
  （identity 語法用 H2 標準的 `GENERATED BY DEFAULT AS IDENTITY`）。

## 狀態轉移矩陣（`TaskStatus.canTransitionTo`）

```
TODO        -> IN_PROGRESS, BLOCKED
IN_PROGRESS -> DONE, BLOCKED
BLOCKED     -> IN_PROGRESS, TODO
DONE        -> IN_PROGRESS
```

同狀態轉同狀態視為合法 no-op（LLM 容易重複呼叫，報錯會卡住對話）。不合法轉移
拋 `BoardException`，訊息明確列出目前狀態與嘗試的目標狀態。全覆蓋測試見
`TaskStatusTest`。

## SSE emitter 生命週期

`SseEmitterRegistry`（`web/` 套件）維護行程內單例的 `CopyOnWriteArrayList`，
不用 Redis pub/sub 或任何跨行程廣播。

- `SseEmitter` timeout 設 `0L`（不逾時）
- 每 15 秒排程送一次 `heartbeat`（`@Scheduled(fixedRate = 15_000)`，需要
  `@EnableScheduling`），避免中介層斷線
- `onCompletion` / `onTimeout` / `onError` 三個 callback 都會把 emitter 從
  集合移除，`send()` 拋 `IOException` 時也會移除，避免累積洩漏
- **不做事件重播**，沒有 `Last-Event-ID` 機制。前端重連後直接重抓一次
  `/api/projects`（及當前 `/board`）即可

## 前端：零建置 CDN 版

**不要主動引入 npm、Vite、SFC、TypeScript、Pinia、Vue Router 或任何前端建置
步驟。** 這是刻意的決定，不是還沒做：

- Vue 3 用 `<script src="https://unpkg.com/vue@3/dist/vue.global.prod.js">`
  的 global build，`Vue.createApp({...}).mount('#app')` + in-DOM template
- 用 Vue 而非純 vanilla 的唯一理由是 `<TransitionGroup>`：任務跨欄位移動時
  需要平滑過渡，vanilla 要手寫 FLIP，`TransitionGroup` 免費提供
- 全部邏輯在單一 `src/main/resources/static/app.js`，`index.html` 直接被
  Spring Boot 的 `static/` 服務，`./mvnw clean package` 自動打包進 jar
- 若模板長到 in-DOM 寫不下、或加入多專案切換／篩選器／任務詳情抽屜，才是
  遷移到 Vite + SFC 的訊號；屆時成本低，因為 REST API 已是乾淨邊界

**`TransitionGroup` 的兩個踩雷點（已修過，別重踩）**：

1. `.task-leave-active { position: absolute; }` 必須搭配父容器
   `position: relative`（見 `.column-body`），否則離場動畫的卡片會飄到
   奇怪位置跟其他卡片文字重疊。
2. Level 2（`LevelTwo` 元件）**必須**用 `:key="currentProjectId"` 強制在
   切換不同專案時整個重新掛載，否則 Vue 會重用同一個元件實例，
   `TransitionGroup` 會把「換了一批完全不同的任務」誤判成「同一批任務的
   增刪移動」，導致跨專案切換時卡片文字重疊。

## Design tokens：`src/main/resources/static/tokens.css`

Andon 狀態板美學（工廠告警板：平時安靜、有事才發亮），不是 Trello 風格的拖拉
看板。**不要把顏色改成通用的 Material/Tailwind 預設色。**

- 底色刻意用深靛藍灰 `--ground:#12151C`，不用純黑（長時間注視對比過硬）
- 四種狀態色直接取自 andon 燈號慣例：`--st-todo`（低彩度灰藍）、
  `--st-progress`（琥珀）、`--st-blocked`（紅）、`--st-done`（綠）
- 字體：Archivo（Display，專案名/欄位計數）、IBM Plex Sans（Body）、
  IBM Plex Mono（Data，ID/時間戳/狀態標籤）。**不要換成 Inter 或系統預設字體**，
  Plex 系列與 andon 的工業／技術意象是刻意搭配的
- Activity Rail（左緣 3px 掃光）是整個設計的 signature，兩層都要有，
  600ms 掃光是唯一的「大動作」；其餘一律安靜（無 hover 放大、無進場淡入）
- `@media (prefers-reduced-motion: reduce)` 已實作，退化成純背景色切換

## 常用指令

建置：

```bash
./mvnw clean package
```

跑測試：

```bash
./mvnw test
```

本機直接跑 server（手動測試 stdio 是否乾淨，或搭配 IntelliJ Run 更方便看
log／設中斷點；瀏覽器開 `http://localhost:8080` 看畫面，Ctrl+D 結束 stdin）：

```bash
java -jar target/ai-project-board-backend-1.0.0.jar
```

## 重新接線 Claude Desktop

1. `./mvnw clean package`
2. 編輯 `~/Library/Application Support/Claude/claude_desktop_config.json`：

```json
{
  "mcpServers": {
    "project-board": {
      "command": "java",
      "args": ["-jar", "/絕對路徑/target/ai-project-board-backend-1.0.0.jar"]
    }
  }
}
```

3. **完全結束並重開** Claude Desktop（不是關視窗，要整個結束 process）。

## 本輪（R2）明確不做

網頁上的任何寫入操作（拖拉、按鈕改狀態、新增任務）、使用者登入/多使用者、
雲端部署、HTTP MCP transport、Docker、MySQL、跨機器資料同步、subagent 設定、
事件重播/`Last-Event-ID`、前端單元測試。以上皆為後續 round 的範圍。
